<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Usuarios Registrados</title>

    <!-- Bootstrap CDN para mejorar diseño -->
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >
</head>

<body class="bg-light">

    <div class="container mt-5">
        <h1 class="text-center mb-4">👥 Usuarios Registrados</h1>

        <div class="d-flex justify-content-between mb-3">
            <a href="/" class="btn btn-secondary">🏠 Volver al Inicio</a>
            <a href="/usuarios/create" class="btn btn-success">➕ Agregar Usuario</a>
        </div>

        <table class="table table-striped table-hover shadow rounded">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Correo</th>
                    <th>Teléfono</th>
                    <th>Tipo</th>
                    <th>Acciones</th>
                </tr>
            </thead>

            <tbody>
                <% usuarios.forEach(usuario => { %>
                <tr>
                    <td><%= usuario.id_usuario %></td>
                    <td><%= usuario.nombre %></td>
                    <td><%= usuario.correo %></td>
                    <td><%= usuario.telefono %></td>
                    <td><%= usuario.tipo %></td>
                    <td>
                        <a href="/usuarios/edit/<%= usuario.id_usuario %>" class="btn btn-warning btn-sm">✏ Editar</a>
                        <a href="/usuarios/delete/<%= usuario.id_usuario %>" class="btn btn-danger btn-sm">🗑 Eliminar</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS opcional -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
